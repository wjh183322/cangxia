# 藏匣 Windows 部署说明

藏匣是装在你电脑上的软件，不是网站。登录、读收藏、下载都在本机完成，文件写到你选的磁盘或已映射的 NAS。

需要：**64 位 Windows 10 / 11**。打包机还要装 **Node.js 22 LTS**（[https://nodejs.org](https://nodejs.org)，勾选 “Add to PATH”）。

---

## 一、你只想安装使用

如果你已经拿到安装包（`藏匣 Setup 0.1.0.exe`）或绿色包（`藏匣-0.1.0-win.zip`）：

### 安装包（NSIS）

1. 双击 `藏匣 Setup 0.1.0.exe`。
2. 若 SmartScreen 提示「未知名发布者」，选 **仍要运行**（个人软件未买代码签名证书时常见）。
3. 选安装目录，完成安装。
4. 开始菜单打开 **藏匣**。

### 绿色包（zip）

1. 解压整个文件夹，不要只拖出一个 exe。
2. 进入解压目录，双击 `藏匣.exe`。
3. 以后升级：关掉软件，用新 zip 覆盖同一文件夹。

### 第一次使用

1. 点 **扫码登录**，用自己的抖音 App 扫码（只登录自己的号）。
2. 设置里选 **下载根目录**（本机磁盘，或已映射成盘符的 NAS，例如 `Z:\藏匣`）。
3. 点 **刷新**，等收藏读进来；条数够了可点停止。
4. 在收藏里勾选，或填「从第 N 条到第 N+M 条」，点下载。
5. 顶栏 **下载** 看进度；图库里浏览、筛选、删除本机备份。

出现滑块验证码：电脑会通知（可设微信推送）。回到这台电脑，在弹出的抖音窗口里完成滑块，再点继续。

杀毒软件可能拦截 Electron 打包的 exe，加入白名单即可。不要把 SMB 端口映射到公网；出门用 NAS 请先 VPN 组网再映射盘符。

---

## 二、从源码自己打包（推荐在 Windows 上做）

源码： [https://github.com/wjh183322/cangxia](https://github.com/wjh183322/cangxia)

### 1. 安装 Node.js

安装 **22 LTS**，新开一个「命令提示符」或 PowerShell，检查：

```bat
node -v
npm -v
```

应显示 `v22.x` 和 npm 版本号。

### 2. 拿到源码

**方式 A · Git**

```bat
git clone https://github.com/wjh183322/cangxia.git
cd cangxia
```

**方式 B · 下载 zip**

GitHub 仓库页 Code → Download ZIP，解压后进入解压出的 `cangxia-main` 文件夹。

### 3. 安装依赖并生成界面

在**项目根目录**（能看到 `package.json` 和 `desktop` 文件夹）：

```bat
npm install
npm run build:desktop
```

成功后会出现 `desktop\ui\index.html`。没有这个文件，窗口会提示界面缺失。

### 4. 安装本机层并打包

```bat
cd desktop
npm install
npm run pack:win
```

这一步会下载 Electron，大约几百 MB，只需第一次。

### 5. 成品位置

```
desktop\dist-win\
  藏匣 Setup 0.1.0.exe     ← 发给自己安装用
  藏匣-0.1.0-win.zip       ← 绿色免安装
```

把这两个拷到 U 盘或网盘即可。不要把整个 `node_modules` 发出去。

### 6. 本地先试跑（不打包）

```bat
cd desktop
npm start
```

应出现「藏匣」窗口。没有窗口先确认第 3 步的 `desktop\ui` 在。

---

## 三、打包时常见问题

| 现象 | 处理 |
|---|---|
| `npm` 不是内部命令 | Node 没进 PATH，重装并勾选 Add to PATH，重开终端 |
| `界面文件缺失` | 回到根目录执行 `npm run build:desktop` |
| `electron-builder` 失败 / 缺 Wine | 必须在 **Windows** 上执行 `npm run pack:win`，不要在 Linux 上打 NSIS |
| 杀毒删除 exe | 加白名单，或用 zip 绿色包 |
| 打开是白屏 | `desktop\ui` 没打进包。检查 `desktop/package.json` 里 `build.files` 含 `ui/**` |
| 扫码窗口打不开 | 检查网络能访问抖音；关代理试试 |
| 下载写不进盘 | 换一个有权限的根目录；NAS 要先在资源管理器里能打开那个盘符 |

---

## 四、目录说明（给你对照）

```
cangxia/
  package.json              根脚本（含 build:desktop）
  vite.desktop.config.ts    把界面打成桌面文件
  src/                      界面源码
  desktop/
    main.mjs                窗口、登录、下载落盘
    preload.cjs
    lib/                    本机逻辑
    ui-src/                 桌面入口
    ui/                     生成结果，打进 exe（不提交 git）
    dist-win/               安装包输出
  部署说明.md                本文件
```

演示封面没有进仓库。正式使用只登录自己的号，刷新后才是你的收藏。
